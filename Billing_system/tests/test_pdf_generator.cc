// test_pdf_generator.cc
#include "pdf_generator.h"
#include <iostream>

/*
int main() {
    // Datos de prueba
    std::string clientName = "John Doe";
    std::vector<Product> products = {
        {"Product 1", 19.99, 2},
        {"Product 2", 5.49, 5},
        {"Product 3", 7.99, 1}
    };

    // Crear un generador de PDF
    PDFGenerator pdfGen;

    // Generar el PDF
    if (pdfGen.createPDF("invoice.pdf", clientName, products)) {
        std::cout << "PDF generated successfully: invoice.pdf" << std::endl;
    } else {
        std::cerr << "Failed to generate PDF." << std::endl;
    }

    return 0;
}
*/